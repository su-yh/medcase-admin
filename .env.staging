# 页面标题
VITE_APP_TITLE = 若依管理系统

# 生产环境配置
VITE_APP_ENV = 'staging'

# 部署时管理端静态资源和前端路由的访问前缀
VITE_APP_BASE_PATH = '/medcase-admin/'

# 若依管理系统/生产环境
VITE_APP_BASE_API = '/stage-api/medcase'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
